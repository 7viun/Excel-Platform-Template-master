<Window x:Class="UI.Views.ExportCSV.ExportDrawingListMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="1280" MinWidth="1280" Height="1024" MinHeight="1024" ResizeMode="CanResizeWithGrip"
             WindowStartupLocation="CenterScreen"
             Title="Export Drawing List" 
             Icon="pack://application:,,/Resources;component/Images/Icons/Logo.Artelia.16x16.ico">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../App.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <DockPanel  Grid.Row="0">
            <StackPanel Margin="0" Name="Stackpane" DockPanel.Dock="Top">
                <TextBlock 
                    Margin="10,5,10,0" 
                    HorizontalAlignment="Center"
                    Style="{StaticResource BaseTextBlockStyle}"
                    Text="PROJECT NAME"></TextBlock>
                <TextBox
                TextWrapping="Wrap"
                HorizontalAlignment="Stretch"
                Margin="20,0,20,5"
                TextAlignment="Center" Text="{Binding ProjectName}"></TextBox>
            </StackPanel>
        </DockPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border Margin="20,0,0,0" BorderBrush="Black" BorderThickness="0,0,1,0" Grid.Column="0">
                <StackPanel Grid.Column="0">
                    <TextBlock TextAlignment="Center">asdasd</TextBlock>
                    <TextBox BorderThickness="1,1,0,1" TextAlignment="Center">asdasd</TextBox>
                </StackPanel>
            </Border>
            <Border Margin="0,0,20,0" BorderBrush="Black" Grid.Column="1">
                <StackPanel Grid.Column="1">
                    <TextBlock TextAlignment="Center">asdasd</TextBlock>
                    <TextBox BorderThickness="0,1,1,1" TextAlignment="Center">asdasd</TextBox>
                </StackPanel>
            </Border>
        </Grid>
        <Grid Grid.Row="2">
            <StackPanel>
                <Button Command="{Binding SaveCommand}">Save</Button>
                <Button Command="{Binding LoadCommand}">Load</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
